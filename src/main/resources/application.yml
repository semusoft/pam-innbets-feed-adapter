server:
  address: 0.0.0.0
  port: 8089

  servlet:
    context-path: /api/v1/unified/feed

logging:
  level:
    com.sportradar: DEBUG
    com.pam.sportradar: DEBUG

spring:

  jackson:
    serialization:
      fail-on-empty-beans: false
    default-property-inclusion: NON_NULL
    deserialization:
      fail-on-unknown-properties: false

  kafka:
    producer:
      properties:
        max.request.size: 100485760  # 100MB
    consumer:
      properties:
        fetch.max.bytes: 100485760  # 100MB
    listener:
      missing-topics-fatal: false

  application:
    name: orchestrator
  profiles:
    active: dev

  logging:
    level:
      root: INFO
      org.apache.kafka.clients.consumer: WARN
      org.apache.kafka.clients.producer: WARN
      org.apache.kafka.clients.admin: WARN
      org.apache.kafka: ERROR
      org.springframework.kafka: WARN

  datasource:
    url: jdbc:mysql://10.10.2.214:3306/feed?allowPublicKeyRetrieval=true&serverTimezone=UTC&useSSL=false
    username: dev
    password: cacate11
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
    # ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  doc:
    api-docs:
      enabled: true
    swagger-ui:
      enabled: true
  data:
    redis:
      host: 10.10.2.211
      port: 6379

# Sport Radar Feed Access
sdk:
  livescout:
    enabled: true
    username: sportTechSDK_User
    password: BlfO3PlKLGLWXUx

uof:
  protocol-host: "http://ib33.innbets.com:5005"
  access-token: "sportTechSDK_User"
  api-host: "ib33.innbets.com"
  api-port: 5005
  use-ssl: false
  messaging:
    host: "ib33.innbets.com"
    username: "sportTechSDK_User"
    password: "BlfO3PlKLGLWXUx"
    port: 5671
    use-ssl: true
    virtual-host: "/unifiedfeed/8"
  default-language: "ENGLISH"
  message-interest: "PrematchMessagesOnly"

feed:
  kafka-server:
    topic:
      name: innbets-feed-topic-raw
    bootstrap:
      server: 10.10.2.229

  rest:
    client:
      timeout: 300000
  lang: en

resilience4j.ratelimiter:
  instances:
    recoveryRateLimiter:
      limitForPeriod: 10
      limitRefreshPeriod: 1s
      timeoutDuration: 500ms
